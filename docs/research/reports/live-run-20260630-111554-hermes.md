# Hermes — Coverage Mode Implementation Review `20260630-111554`

> One run, one file. Each agent writes **only its own sections**, never edits the other's.
> Each round = **one core judgment + one next_action + one not_doing**.
> **`Claude Final Decision` is the only section that drives code.**
> md auto-commit/push; raw log / raw JSONL / secrets never committed.

```yaml
run_id: 20260630-111554
date: 2026-06-30
hermes_stage: implementation_review_mid_loop
starting_point: Codex already implemented coverage mode from analysis-plan.md before this Hermes record was created
current_gate: Claude Code Review -> Codex fixes if needed -> Claude Final Approval -> next 30-min live run
window: searcher-strategy-20260630-111554 local run
config:
  mode: dry-run
  mempool: filtered
  live_backend: hybrid
inputs:
  redacted_log: pending
  redacted_events_summary: pending
  coverage_report: analysis/outputs/live-loss/smoke-25427813/coverage-25427813-25427813.md (local, gitignored smoke)
  key_tx_links:
    - https://etherscan.io/tx/0xad639cd3a70b6e0a7fbb6b91fb6d99f5afefac42614f3fc0e746035b08892a31
    - https://etherscan.io/tx/0x9aacade300e0839ba20b01016195230c7bc93c433dcdc36364d585303dd8935f
    - https://etherscan.io/tx/0x498ef20037b4971bdd7bb271db5b3a7f44c2d81f492e66338ab09616005b5e01
```

---

## Run Facts  <!-- auto -->

This is a **mid-loop Hermes record** for the coverage-mode implementation and smoke test.
It is **not** a full Hermes live-run cycle that started from `Run Live -> Auto Analysis -> dual-agent decision`.

Why this starts in the middle:

```text
analysis-plan.md was updated first
-> Codex implemented coverage mode from that plan
-> this Hermes file was created afterward so Claude can review the implementation in the shared record
-> next run should start from the full Hermes live-run template
```

```
funnel: latest raw run had opportunity_seen=14 / unique victims=10 in events
mempool: filtered mempool path was already active in this run
dominant pipeline_dropped reason: not analyzed in this Hermes record
```

## Auto Analysis  <!-- auto -->

- Added production event envelope in `listener/src/searcher/events.ts`.
- Added `analysis live-loss --coverage --watch` run-level competitor coverage mode.
- Smoke-tested block `25427813`, the Jared AI/USDC sample.

## Competitor Coverage  <!-- auto: analysis live-loss --coverage, same window -->

Smoke command:

```bash
cd /Users/eunice/src/MEV/analysis
READONLY_RPC_URL="$MAINNET_RPC_URL" npm run live-loss -- \
  --events ./events/searcher-strategy-20260630-111554.jsonl \
  --coverage \
  --watch 0xae2Fc483527B8EF99EB5D9B44875F005ba1FaE13,0x1f2f10d1c40777ae1da742455c65828ff36df387 \
  --from-block 25427813 \
  --to-block 25427813 \
  --trace-top 0 \
  --output ./outputs/live-loss/smoke-25427813
```

Smoke result:

```text
ours:        opportunity_seen=14 / unique_victims=10 / submitted=0 / included=0
competitor:  executed_total=2 / exact_victim_seen=0 / same_pool_seen=0 / not_seen_by_us=2
classification: sandwich_excluded=2 / non_sandwich_candidate=1 / mixed=1
completeness_pct: 100.00 / partial=false
```

## Path / Leg Findings  <!-- auto -->

Block `25427813`:

- `0xad639...a31` is classified `mixed`.
  - sandwich leg: AI/USDC pre leg in pool `0x3198ca64ebff6d008860f2c450cfcbf1faac7677`.
  - victim: `0x9aacade300e0839ba20b01016195230c7bc93c433dcdc36364d585303dd8935f`.
  - post: `0x498ef20037b4971bdd7bb271db5b3a7f44c2d81f492e66338ab09616005b5e01`.
  - non-sandwich legs are retained separately for replay review.
- `0x498e...5e01` is classified `sandwich_excluded`.

---

## Codex Round 1

- **core judgment:** This file is an implementation-review record inside Hermes, not a full live-run cycle. Use **Hermes** as the protocol name; call repeated 30-minute runs the **Hermes live-run loop**.
- **next_action:** Make Claude write review output into this same Hermes md, specifically `Claude Code Review`, then use `Claude Final Approval` to decide whether this already-written implementation needs fixes.
- **not_doing:** Do not build a full orchestrator yet; first use shared md + GitHub state for 2-3 rounds.

## Claude Round 1

- **core judgment:** pending
- **next_action:** pending
- **not_doing:** pending

## Codex Review Of Claude

- pending

## Claude Review Of Codex

- pending

## Codex Final View

- **core judgment:** The v1 coverage implementation is the right first production-observability cut: it compares our seen opportunities against watchlist bot execution and separates sandwich visibility from non-sandwich path review.
- **next_action:** Ask Claude to review commit `979d126 add live competitor coverage mode` and write findings under `Claude Code Review`.
- **not_doing:** Do not treat sandwich legs as missed pure backrun; do not add dashboard/indexer/orchestrator in this iteration.

## Claude Final View

- **core judgment:** pending
- **next_action:** pending
- **not_doing:** pending

---

## Claude Final Decision  <!-- AUTHORITATIVE — only this drives code -->

- **decision:** pending
- **rationale (1-2 lines):** pending

Scope note: because Codex already wrote commit `979d126` before this Hermes record existed,
Claude's decision here is an **approval/fix gate for the current implementation**, not the
pre-implementation decision that would normally drive code in a full Hermes round.

## Implementation Brief

| task | owner | files | done-when |
|---|---|---|---|
| Review coverage-mode implementation | Claude | `listener/src/searcher/events.ts`, `analysis/src/cli/live-loss.ts`, `docs/research/analysis-plan.md` | Findings written under `Claude Code Review` |
| Fix review findings, if any | Codex | TBD | Build + smoke pass |

## Acceptance Criteria

1. `events.ts` emits `schema_version/run_id/chain_id` without affecting hot path.
2. `live-loss --coverage --watch` emits run-level coverage counts.
3. Block `25427813` smoke classifies `0xad639...` as `mixed`, `0x498e...` as `sandwich_excluded`, and `0x9aac...` as victim.
4. Sandwich legs do not contaminate non-sandwich path candidates.
5. Raw logs, raw JSONL, secrets, and `error.log` are not committed.

---

## Codex Implementation Notes

- Commit under review: `979d126 add live competitor coverage mode`.
- Build checks passed:
  - `cd analysis && npm run build`
  - `cd listener && npm run build`
- Event envelope smoke passed: emitted JSONL row contains `schema_version=1`, `run_id`, `chain_id=1`, and `emitted_at_ms`.
- Coverage smoke passed on block `25427813`.

## Claude Code Review

Claude should fill this section.

Review prompt:

```text
cd /Users/eunice/src/MEV
git pull

Please review commit 979d126 add live competitor coverage mode.

Focus:
1. Does listener/src/searcher/events.ts satisfy docs/research/analysis-plan.md §9 for schema_version/run_id/chain_id without hot-path risk?
2. Does analysis/src/cli/live-loss.ts --coverage --watch satisfy the coverage-mode acceptance criteria?
3. Does block 25427813 classify:
   - 0xad639... as mixed
   - 0x498e... as sandwich_excluded
   - 0x9aac... as victim
   - AI/USDC sandwich leg excluded without dropping non-sandwich legs?
4. Is the same-actor + same-pool + inverse/inventory-close sandwich rule too broad or too narrow?
5. Any secret/raw log/raw JSONL commit risk?
6. Any analysis -> listener/searcher import boundary violation?

Return:
- one core review conclusion
- blocking issues
- P1 issues
- approve / not approve
```

## Codex Final Review

- pending Claude review

## Claude Final Approval

- **approved:** pending
- **follow-ups (if any):** pending

---

## Next Run

- **config changes:** after Claude review approval/fixes, run a new 30-minute live/dry-run and generate a **full** Hermes record from redacted artifacts plus coverage report.
- **what this run should answer:** whether watchlist coverage shows a persistent visibility gap, a path/pool gap, or mostly excluded sandwich/standing activity.
