# Hermes — Coverage Mode Implementation Review `20260630-111554`

> One run, one file. Each agent writes **only its own sections**, never edits the other's.
> Each round = **one core judgment + one next_action + one not_doing**.
> **`Claude Final Decision` is the only section that drives code.**
> md auto-commit/push; raw log / raw JSONL / secrets never committed.

```yaml
run_id: 20260630-111554
date: 2026-06-30
hermes_stage: implementation_review_mid_loop
starting_point: Codex already implemented coverage mode from analysis-plan.md before this Hermes record was created
current_gate: Claude Code Review -> Codex fixes if needed -> Claude Final Approval -> next 30-min live run
window: searcher-strategy-20260630-111554 local run
config:
  mode: dry-run
  mempool: filtered
  live_backend: hybrid
inputs:
  redacted_log: pending
  redacted_events_summary: pending
  coverage_report: analysis/outputs/live-loss/smoke-25427813/coverage-25427813-25427813.md (local, gitignored smoke)
  key_tx_links:
    - https://etherscan.io/tx/0xad639cd3a70b6e0a7fbb6b91fb6d99f5afefac42614f3fc0e746035b08892a31
    - https://etherscan.io/tx/0x9aacade300e0839ba20b01016195230c7bc93c433dcdc36364d585303dd8935f
    - https://etherscan.io/tx/0x498ef20037b4971bdd7bb271db5b3a7f44c2d81f492e66338ab09616005b5e01
```

---

## Hermes Cycle Map

Full Hermes live-run cycle:

```text
1. Run live/dry-run for 30 minutes
2. Auto redact raw log/events
3. Auto generate loss/coverage/path-leg analysis
4. Codex Round 1
5. Claude Round 1
6. Codex Review Of Claude
7. Claude Review Of Codex
8. Codex Final View
9. Claude Final View
10. Claude Final Decision + Implementation Brief + Acceptance Criteria
11. Codex implements
12. Claude Code Review
13. Codex fixes/reviews
14. Claude Final Approval
15. Next 30-minute live run
```

This record starts **mid-cycle**, after step 11. The earlier steps are not removed;
they are marked as retrospective/skipped for this transitional record:

| step | normal owner | status in this file | note |
|---|---|---|---|
| 1. Run live/dry-run | user/Codex | retrospective | uses existing `searcher-strategy-20260630-111554` run |
| 2. Redact artifacts | Codex | skipped | this file does not publish full redacted artifacts |
| 3. Auto analysis | Codex | partial retrospective | only coverage smoke for block `25427813` is recorded |
| 4-10. Dual-agent decision rounds | Codex/Claude | skipped/retrospective | implementation was already written from `analysis-plan.md` |
| 11. Codex implements | Codex | done | commit `979d126` |
| 12. Claude Code Review | Claude | current gate | Claude should fill `Claude Code Review` below |
| 13-14. Fix/final approval | Codex/Claude | pending | depends on Claude review |
| 15. Next live run | user/Codex | pending | should use full Hermes template from step 1 |

Next run should start from step 1 and should not skip the dual-agent decision rounds.

---

## Run Facts  <!-- auto -->

This is a **mid-loop Hermes record** for the coverage-mode implementation and smoke test.
It is **not** a full Hermes live-run cycle that started from `Run Live -> Auto Analysis -> dual-agent decision`.

Why this starts in the middle:

```text
analysis-plan.md was updated first
-> Codex implemented coverage mode from that plan
-> this Hermes file was created afterward so Claude can review the implementation in the shared record
-> next run should start from the full Hermes live-run template
```

```
funnel: latest raw run had opportunity_seen=14 / unique victims=10 in events
mempool: filtered mempool path was already active in this run
dominant pipeline_dropped reason: not analyzed in this Hermes record
```

## Auto Analysis  <!-- auto -->

- Added production event envelope in `listener/src/searcher/events.ts`.
- Added `analysis live-loss --coverage --watch` run-level competitor coverage mode.
- Smoke-tested block `25427813`, the Jared AI/USDC sample.

## Competitor Coverage  <!-- auto: analysis live-loss --coverage, same window -->

Smoke command:

```bash
cd /Users/eunice/src/MEV/analysis
READONLY_RPC_URL="$MAINNET_RPC_URL" npm run live-loss -- \
  --events ./events/searcher-strategy-20260630-111554.jsonl \
  --coverage \
  --watch 0xae2Fc483527B8EF99EB5D9B44875F005ba1FaE13,0x1f2f10d1c40777ae1da742455c65828ff36df387 \
  --from-block 25427813 \
  --to-block 25427813 \
  --trace-top 0 \
  --output ./outputs/live-loss/smoke-25427813
```

Smoke result:

```text
ours:        opportunity_seen=14 / unique_victims=10 / submitted=0 / included=0
competitor:  executed_total=2 / exact_victim_seen=0 / same_pool_seen=0 / not_seen_by_us=2
classification: sandwich_excluded=2 / non_sandwich_candidate=1 / mixed=1
completeness_pct: 100.00 / partial=false
```

## Path / Leg Findings  <!-- auto -->

Block `25427813`:

- `0xad639...a31` is classified `mixed`.
  - sandwich leg: AI/USDC pre leg in pool `0x3198ca64ebff6d008860f2c450cfcbf1faac7677`.
  - victim: `0x9aacade300e0839ba20b01016195230c7bc93c433dcdc36364d585303dd8935f`.
  - post: `0x498ef20037b4971bdd7bb271db5b3a7f44c2d81f492e66338ab09616005b5e01`.
  - non-sandwich legs are retained separately for replay review.
- `0x498e...5e01` is classified `sandwich_excluded`.

---

## Codex Round 1

- **core judgment:** This file is an implementation-review record inside Hermes, not a full live-run cycle. Use **Hermes** as the protocol name; call repeated 30-minute runs the **Hermes live-run loop**.
- **next_action:** Make Claude write review output into this same Hermes md, specifically `Claude Code Review`, then use `Claude Final Approval` to decide whether this already-written implementation needs fixes.
- **not_doing:** Do not build a full orchestrator yet; first use shared md + GitHub state for 2-3 rounds.

## Claude Round 1

- **core judgment:** pending
- **next_action:** pending
- **not_doing:** pending

## Codex Review Of Claude

- pending

## Claude Review Of Codex

- pending

## Codex Final View

- **core judgment:** The v1 coverage implementation is the right first production-observability cut: it compares our seen opportunities against watchlist bot execution and separates sandwich visibility from non-sandwich path review.
- **next_action:** Ask Claude to review commit `979d126 add live competitor coverage mode` and write findings under `Claude Code Review`.
- **not_doing:** Do not treat sandwich legs as missed pure backrun; do not add dashboard/indexer/orchestrator in this iteration.

## Claude Final View

- **core judgment:** pending
- **next_action:** pending
- **not_doing:** pending

---

## Claude Final Decision  <!-- AUTHORITATIVE — only this drives code -->

- **decision:** pending
- **rationale (1-2 lines):** pending

Scope note: because Codex already wrote commit `979d126` before this Hermes record existed,
Claude's decision here is an **approval/fix gate for the current implementation**, not the
pre-implementation decision that would normally drive code in a full Hermes round.

## Implementation Brief

| task | owner | files | done-when |
|---|---|---|---|
| Review coverage-mode implementation | Claude | `listener/src/searcher/events.ts`, `analysis/src/cli/live-loss.ts`, `docs/research/analysis-plan.md` | Findings written under `Claude Code Review` |
| Fix review findings, if any | Codex | TBD | Build + smoke pass |

## Acceptance Criteria

1. `events.ts` emits `schema_version/run_id/chain_id` without affecting hot path.
2. `live-loss --coverage --watch` emits run-level coverage counts.
3. Block `25427813` smoke classifies `0xad639...` as `mixed`, `0x498e...` as `sandwich_excluded`, and `0x9aac...` as victim.
4. Sandwich legs do not contaminate non-sandwich path candidates.
5. Raw logs, raw JSONL, secrets, and `error.log` are not committed.

---

## Codex Implementation Notes

- Commit under review: `979d126 add live competitor coverage mode`.
- Build checks passed:
  - `cd analysis && npm run build`
  - `cd listener && npm run build`
- Event envelope smoke passed: emitted JSONL row contains `schema_version=1`, `run_id`, `chain_id=1`, and `emitted_at_ms`.
- Coverage smoke passed on block `25427813`.

## Claude Code Review

**Reviewer:** Claude (Claude Code). Verified against code, not the implementer's notes (Hermes governance). Commit `979d126` (coverage mode) + the envelope now on HEAD `3ba6121`.

**Core review conclusion:** coverage mode is correct and safe on every hard §9 point I checked — keep it. One real **P1**: the headline `router_not_watched` / `private_or_builder_only` buckets are listed in the priority but **never produced** by any code path, so the most actionable attribution — "we missed this victim because its router wasn't in our mempool filter" — currently collapses to `unknown`.

**Verified ✓**
- §9.1 envelope (focus 1): `events.ts:117-119` emits `schema_version`(const 1)/`run_id`/`chain_id` **inside the fire-and-forget try/catch** → hot-path safe. Satisfied.
- §9.5 sandwich (focus 4): `detectLocalSandwiches` = same-actor (`sameActor`) + shared pool + `inventoryCloses` + a victim found between pre/post on that pool (`findVictimBetween`). Conservative, not too broad; cross-actor never merged → satisfies the `false-sandwich-linkage` intent.
- §9.9 postState (focus 2): `buildCoverageMatch:580` only yields `requires_replay`/`no`; **logs-only never yields `yes`** ✓.
- §9.4 priority order: `coverageReasons` priority array matches spec order.
- focus 6: no `analysis → listener/searcher` import ✓. focus 5: outputs gitignored, builders hashed, no raw log/JSONL/key in this change ✓.

**P1 (not blocking the merge; blocks the router-attribution goal)**
- `router_not_watched` / `private_or_builder_only` are **dead buckets**: `coverageReasons` only ever pushes `standing_not_victim_triggered / unknown / pool_not_in_graph / token_pair_not_in_universe / seen_pool_but_no_plan`. The two router/private reasons sit in the `priority` array with no producer. Root cause: coverage mode doesn't load the searcher's mempool-filter `toAddress` set, so it can't decide "victim.to ∉ filter"; a never-seen victim collapses to `unknown`. **Fix:** feed coverage the searcher filter set (routers + pools, e.g. a generated registry) + a private-flow signal, then map "block-not-seen + victim.to ∉ filter → router_not_watched", "tx never public → private_or_builder_only".

**P2 / verify**
- `inventoryCloses`: confirm it checks opposite-direction + a tolerance (§9.5 ≈±1%), not just net≈0.
- cross-actor sandwiches are skipped entirely (no `sandwich_suspect`) — acceptable/conservative for v1; note as known limitation.
- focus 3 (Jared `25427813`): acceptance asserts the mixed / sandwich_excluded / victim split, but per the cycle map this was a retrospective smoke and I did **not** re-run coverage on it in this review (CU). Verify on the next coverage run.

**Approve:** ✅ approve the coverage mode (correct + safe; the P1 is a not-yet-implemented part of §9.4, not a regression). The `router_not_watched` attribution should be the **first follow-up** before we rely on coverage to tell us "which router to add".

Review prompt:

```text
cd /Users/eunice/src/MEV
git pull

Please review commit 979d126 add live competitor coverage mode.

Focus:
1. Does listener/src/searcher/events.ts satisfy docs/research/analysis-plan.md §9 for schema_version/run_id/chain_id without hot-path risk?
2. Does analysis/src/cli/live-loss.ts --coverage --watch satisfy the coverage-mode acceptance criteria?
3. Does block 25427813 classify:
   - 0xad639... as mixed
   - 0x498e... as sandwich_excluded
   - 0x9aac... as victim
   - AI/USDC sandwich leg excluded without dropping non-sandwich legs?
4. Is the same-actor + same-pool + inverse/inventory-close sandwich rule too broad or too narrow?
5. Any secret/raw log/raw JSONL commit risk?
6. Any analysis -> listener/searcher import boundary violation?

Return:
- one core review conclusion
- blocking issues
- P1 issues
- approve / not approve
```

## Codex Final Review

**Reviewer:** Codex. Verified against `analysis/src/cli/live-loss.ts`,
`listener/src/searcher/events.ts`, and `docs/research/analysis-plan.md` after
Claude's review commit `43ab31f`.

**Core judgment:** I agree with Claude's approval and with the P1. Coverage mode
is safe to keep, but it is not yet able to answer the most actionable coverage
question: "did we miss this because the victim router was not watched?"

**What I verified**
- `events.ts` does emit `schema_version/run_id/chain_id` inside the existing
  fail-open emitter path, so the envelope concern is closed.
- `coverageReasons` has the §9.4 priority order, but only produces:
  `standing_not_victim_triggered`, `unknown`, `pool_not_in_graph`,
  `token_pair_not_in_universe`, and `seen_pool_but_no_plan`.
- `router_not_watched` and `private_or_builder_only` are present only in the
  priority list/spec. No code path pushes either reason today. Claude's "dead
  bucket" finding is real.
- `inventoryCloses` checks opposite-direction deltas and requires net drift
  `<= 1%` of the larger leg for at least two tokens, so the confirmed-sandwich
  rule is conservative enough for v1.

**P1 accepted**
- The first follow-up should add a coverage-side view of the searcher's filtered
  mempool `toAddress` set, then classify:
  `has victim + victim_to not in filter -> router_not_watched`.
- `private_or_builder_only` should remain conservative until there is evidence
  that the victim never appeared in public mempool; do not infer it from logs
  alone.

**Next action:** make the router-filter set explicit and testable, preferably as
a generated/read-only registry shared by searcher and analysis, then add a
router-miss fixture where `victim_to` is outside the filter and the primary
reason is `router_not_watched`.

**Not doing:** do not start the next full Hermes live-run assuming coverage can
already tell us which router to add; without this P1 it can still collapse those
misses to `unknown`.

## Claude Final Approval

(step 14 — done **after** Codex Final Review / step 13.)

- **Codex step 13 acknowledged:** Codex agreed with the approval + P1, made **no code changes** (P1 deferred to follow-up by mutual agreement), and **confirmed `inventoryCloses` checks opposite-direction + ≤1% drift** → my P2 verify item is resolved (sandwich rule is correct as written).

- **Go-live safety of the code: ✅ yes.** Re-checked the post-review code: `listener` builds clean (`tsc`); the only hot-path change this batch is the `events.ts` envelope, inside the fail-open emitter (verified). Coverage mode is offline → does not touch the searcher. The next live run is **safe to start** on this code.

- **But gate the next run's VALUE on P1 first.** Without `router_not_watched`, the next live's coverage collapses not-seen misses to `unknown` → we won't learn "which router to add" (the #1 actionable output). The node is still syncing, so there is time: **land P1 (Codex's scoped fix — shared read-only filter registry + router-miss fixture) before the first full-speed live run.**

- **approved:** ✅ **go-live-safe.** Recommended order: **P1 (router attribution) during node sync → then the first full Hermes live run** so its coverage is actionable, not router-blind.
- **remaining P2 (non-blocking):** cross-actor `sandwich_suspect` not flagged (conservative, fine for v1); re-verify the Jared `25427813` split on the next real coverage run. (`inventoryCloses` P2 resolved by Codex.)

---

## Next Run

- **config changes:** after Claude review approval/fixes, run a new 30-minute live/dry-run and generate a **full** Hermes record from redacted artifacts plus coverage report.
- **what this run should answer:** whether watchlist coverage shows a persistent visibility gap, a path/pool gap, or mostly excluded sandwich/standing activity.
