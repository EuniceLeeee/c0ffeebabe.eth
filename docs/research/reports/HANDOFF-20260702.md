# Session handoff — Uniswap v4 arbitrage coverage + latency (2026-07-02)

> **Scope / context.** This is authorized, defensive DeFi research: replicating and studying on-chain arbitrage (capturing price dislocations that already exist across pools) on a **mainnet fork and in dry-run only**. It reads public on-chain data and simulates trades locally; it does **not** target or harm any user. Signing/broadcasting any transaction is a hard human gate and has not been done. Terms like "backrun", the code symbol `victimApply`, etc. are standard DeFi/MEV nomenclature for the price-moving trade an arbitrage follows — not adversarial action against a person.

The previous session ran an autonomous arbitrage-coverage + latency pass (R1–R7). **Continue from here with full agency** — the open problems in §3 are yours to own end-to-end (diagnose → design → implement → gate → merge), not to triage or defer. You have the same authority and tools as the prior session; treat its choices as reviewable, not fixed. Below is the state, the open problems, and exactly where to look.

## 1. Read first (context, in order)
1. **`CLAUDE.md`** — Hermes governance rules 1–14. Critical: **11** (Codex = gpt-5.5 xhigh generator, `codex exec -o <file>`, judge by output file not stdout), **12** (repair-replay gate: searcher fixes need a replay flip; latency/inclusion exempt → metrics), **13** (forcing functions, ≤1 observability turn), **14** (multi-round = user away → decide + record, don't `AskUserQuestion`).
2. **`docs/research/reports/live-run-20260701-detgap-hermes.md`** — full ledger of this run (R1–R7: run facts, competitor cross-ref, every finding + decision + the Findings Ledger).
3. **Memory** (`~/.claude/projects/-Users-eunice-src-MEV/memory/`, index `MEMORY.md`): `project-univ4-coverage-frontier` (the whole v4 story + blockers), `project-v8-quote-loop-bottleneck` (known quote/latency depth), `reference-codex-background-suspend` (caffeinate), `feedback-multiround-user-away-autonomy` (rule 14), `feedback-validate-live-not-backtest`.

## 2. Done this run (R1–R7, all merged `main`, Codex-reviewed + gated)
| R | change | gate |
|---|---|---|
| R1 | `quoteUniV4` → real V4Quoter + pin USDC/USDT fee8 | reproduces real v4 swap 0 bps (`test/validate-v4-quote.ts`) |
| R2 | no_candidate root-cause (80% longtail; disproved "pin v4 fixes 78%") | on-chain competitor cross-ref |
| R3 | **v4-impact-detection** (`uniV4Decoder`, v4 Swap→PoolImpact by poolId) | `test/v4-impact-detect.ts` |
| R4 | solver v4 identity (`sameV4Pool` poolId guard) | tsc + planner regression |
| R5 | 2nd v4 pool (fee7) + **execution PoolKey from `edge.v4PoolKey`** (was hardcoded fee100) | `test/v4-execution-poolkey.ts` |
| R6 | **v4 dry-run — PRODUCTION VALIDATED** | a v4 pool swap → `opportunity_seen` live (before: 0/80) |
| R7 | arb-profit WETH→ETH unwrap double-count fix (competitor sizing was ~2×) | `analysis/src/test/weth-unwrap-delta.ts` |

**Key win:** the v4 detection→quote→identity→execution chain is complete and validated in the live pipeline (R6). Codex review caught 3 real blockers along the way (identity collapse, planner path dedupe, hardcoded execution fee).

## 3. OPEN PROBLEMS (for Fable) — ranked

### A. Latency root-cause fix — **NOT the TTL band-aid** (user's explicit follow-up)
- **Symptom:** opps die `expired-before-solver` (killed our one live v4 opp in R6).
- **What the measurements actually show (two dry-run windows, TTL 5000 then 8000):** the expired opps' measured stages `match+fork+prep+detect+plan` sum to only **~130–250ms**, but `total` = the full TTL (5003ms at TTL5000; **8022–8495ms at TTL8000**). i.e. the **~5–8s is DEAD TIME not captured by any segMark** — and **`plan` is NOT it** (expired opps have `plan=8–25ms`). **Bumping the TTL did NOT fix it — the dead time just grew with the TTL** (still 3 expired at TTL8000, `solverEntered=52`, `simSuccess=0`).
- **Most likely root cause (verify first):** the **per-hint TTL is SHARED across a serial multi-opp loop** (`main.ts:1112` `for (const opp of opportunities)`; `startedAt=tHint` set once per hint at `main.ts:738`). The cost is the **SOLVER**, not the planner: `SEARCHER_SOLVER_DEADLINE_MS=8000` (`main.ts:300`) lets each opp's `solver.solve` burn up to 8s, and `simSuccess=0` means it runs to the full deadline finding nothing. So opp #1's solve eats the shared budget and opp #2/#3 expire with tiny individual stages. (The `plan=2.8–4.8s` tail seen on a few opps is a *secondary*, separate cost — real but not what killed the measured expired opps.)
- **Correcting the earlier hypothesis:** "planner time budget" is NOT the fix — the planner is bounded (`maxPoolsPerToken=8`, `maxHops=3` → ≤512 paths) and `plan` was fast on every expired opp. Do not chase the planner first.
- **First step — instrument the DEAD TIME (it's currently unmeasured):** `segMark` covers `fork/match/prep/detect/plan` but the gap between `plan` (`main.ts:1142`) and the per-candidate TTL check (`main.ts:1324`) — i.e. `victimApply` (1218), `prepareVictimState`/`overlay` (1261/1262), and **the per-opp `solve` (1350)** — never shows in the expired `segStr`. Add timing there + log the **opportunities-per-hint count** to confirm the multi-opp-starvation theory.
- **Likely fixes (once confirmed):** (1) **give each opportunity its own time budget** instead of one shared per-hint TTL (reset the clock per opp, or divide the budget); (2) **shorten `SEARCHER_SOLVER_DEADLINE_MS`** (8s is huge — a solve that will `simSuccess=0` should bail fast); (3) **early-exit the solver** on a cheap no-profit quote before full GSS/sim; (4) optionally cap opportunities processed per hint.
- **Files:** `listener/src/searcher/main.ts` (opp loop `1112`, TTL check `1324`, `startedAt=tHint` `738`, `segMark`/`segStr`, solver call `1338` with `deadlineMs`), `listener/src/searcher/solver/solver.ts` (`solve`, `deadlineMs`, GSS/finalSim), config `main.ts:300` (`solverDeadlineMs`), `main.ts:301` (`oppTtlMs`).
- **Gate:** metrics (rule 12 latency exemption) — `expired-before-solver` count + `solverEntered`/`simSuccess` + per-stage (incl. the newly-instrumented solve) before/after. Logs at **`/var/log/mev-live.log`** (`grep "opportunity expired"` → `stage breakdown`); events `/var/log/mev/events/searcher-live.jsonl`.
- **Node config note:** I set `SEARCHER_OPP_TTL_MS=8000` in `/opt/MEV/.env` to test — **reverted to default (5000)** since it didn't help. Baseline is clean.

### B. native-ETH v4 execution — the ETH-paired **92%** (user-present + broadcast gate)
- **Blocker:** top v4 volume is native-ETH pairs (ETH/USDC fee100 `0x00b9edc1`, ETH/USDT fee500 `0x72331fcb` — `currency0 = 0x0`). Executing them needs `settle{value: amountIn}()`, but `plan-builder` builds the input leg as `univ4-sync + erc20-transfer + univ4-settle` with **no value path**. Native ETH must be paid via `msg.value` on settle → needs a value-forwarding path in **`src/BotVM.sol` (the executor CONTRACT) + recompile + redeploy**.
- **Files:** `listener/src/searcher/solver/plan-builder.ts` (`univ4-unlock` case ~236), `listener/src/adapters/univ4.ts` (settle adapter ~134), `src/BotVM.sol`.
- **Gate:** fork test — ETH→USDC v4 swap builds + executes on-fork with correct output. **Broadcast stays a HUMAN gate** (do NOT broadcast).

### C. Broad v4 coverage
- Only USDC/USDT (fee7/fee8) pinned → catches a thin stable-stable slice. The 43%/92% is ETH-paired → **blocked on (B)**. After (B): broad-pin the top ETH v4 pools or auto-index the singleton. Curated ERC20/ERC20 v4 pins alone are low value.

### D. Minor
- `defaultTokenGraph()` still has a hardcoded DAI/USDT v4 fee fallback (test/AC-3 only, not the pinned exec path). Codex caveat.

## 4. Current state (as of handoff)
- **Branch:** `main`, everything merged (`git log` top: `65d1325`). No open feature branches needing merge.
- **Node (EC2 `i-0ff908dedeec9ebc6`, SSM only, no SSH):** `mev-searcher.service` **ACTIVE**, **dry-run** (`SEARCHER_DRY_RUN=1`), backend=revm, mempool=1, on local reth `127.0.0.1:8545/8546`. Deployed `main` `137e92d`+ TTL bump. Log `/var/log/mev-live.log` (~382MB — consider rotating), events `/var/log/mev/events/searcher-live.jsonl`. **A TTL=8000 measurement window was in-flight at handoff** — check the log for its `expired` count.
- **Local Mac:** `caffeinate -dimsu -t 18000` guard running (won't sleep for ~5h; kill `pkill caffeinate` or let it expire). This is what stops codex bg runs from suspending.
- **go-live / broadcast:** NEVER performed. Hard human gate.

## 5. Process notes
- **Codex loop (generator/evaluator split, rule 11):** you (the orchestrator) author the v4 code and run the gates; Codex (gpt-5.5 xhigh) is the non-author reviewer. Invoke: `caffeinate -i codex -s workspace-write -a never exec -C /Users/eunice/src/MEV --json -o /tmp/codex-<x>.out "$BRIEF" < /dev/null > /tmp/codex-<x>.events.jsonl 2>&1`. Judge by the `-o` file + `git diff`, never stdout. Don't poll-kill; caffeinate prevents the 3h screen-lock suspend.
- **Verify-before-claim** + **cross-reference on-chain before calling a drop non-arbable** (the R2 lesson: most no_candidate is longtail, but confirm competitor take first).
- **Node RPC = free (local reth, zero Alchemy CU)**; use Alchemy (`$MAINNET_RPC_URL`) only for archive/old-logs (v4 Initialize poolId→PoolKey).
