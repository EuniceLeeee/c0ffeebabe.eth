# Hermes — Live Run `20260701-032600`

> One run, one file. Each agent writes **only its own sections**, never edits the other's.
> Each round = **one core judgment + one next_action + one not_doing**.
> **`Claude Final Decision` is the only section that drives code.**
> md auto-commit/push; raw log / raw JSONL / secrets never committed.

```yaml
run_id: 20260701-032600
date: 2026-07-01
window:        # mev-searcher.service run on mev-node (local reth), ~ended 03:26 UTC
config:        # SEARCHER_LIVE_BACKEND=revm / mempool=1 / OPP_TTL_MS=5000 / SOLVER_DEADLINE_MS=8000
cu_budget:     # 0 Alchemy CU (local reth hot path) — subscription 5h window is the backstop
cu_spent:      # cuProxyRpcCalls=0 (zero Alchemy CU this run)
inputs:
  redacted_events_summary: /var/log/mev/events/searcher-live.jsonl (54 events, desensitized at source)
  run_live_step: systemctl start/stop mev-searcher   # canonical live step
  key_tx_links: []   # longtail pools, listed inline below
```

---

## Run Facts  <!-- auto -->

```
funnel: hints 10343 / impacts 1545 / opportunities 27 / plans 63 / solverEntered 27 / solverSuccess 0 / simSuccess 0 / submitAttempts 0
        expiredBeforeSolver 3 / quoteTimeouts 2 / simReverts 0 / missingState 0 / revmErrors 0
mempool: pendingReceived 9976 / pendingFilteredReceived 111 / cuProxyRpcCalls 0
dominant pipeline_dropped reason: plan | no_candidate_plans (20/26 = 77%)
```

## Auto Analysis  <!-- auto -->

Structured `pipeline_dropped` (events, not log substrings), 26 events:

| count | stage \| reason |
|---|---|
| 20 | `plan \| no_candidate_plans` |
| 3  | `solver \| no-profitable-quote` |
| 3  | `solver \| expired-before-solver` |

`no_candidate_diagnostic` split (all `no_borrowable=0`, `constraint_pass=0` → NOT flash-borrow / template / borrowability — pure token-graph coverage):
- **A `only_immediate_same_pool_reverse`** (majority; single pool `0xE2a1437…`/token_out `0xc470f5E6` recurs **7×**): `impact_token_return_venues_excluding_impact_pool = 0` → impacted token has no second return venue in our graph; reverse leg is same-pool immediate round-trip → correctly pruned → no cycle.
- **B `impact_pool_not_in_routing_graph`** (4 distinct pools `faf41f`/`a8e56`/`9e0905`/`8118ee`): `impact_pool_edge_in_routing_graph = false` → detected but pool below the `minSwaps` activity filter, so its edge isn't in the routing graph. `raw_paths` exist (142) but 0 pass through the impact pool → focused=0.

Both buckets = the impacted token (`0x9d70bae`/`0x1ae560e`/`0x822c71a`/`0xc470f5E6`, all longtail) has no usable second venue in our routing graph → **genuine longtail, mostly non-arbable by a simple flash-swap-repay cycle.** Matches the [[project-jared-benchmark-three-buckets]] longtail bucket.

Sim outcomes: 0 `simulation_result` events emitted (nothing reached sim). `simReverts=0`, `revmErrors=0` → nothing failed in revm; the loss is upstream of sim.

## Competitor Coverage  <!-- auto: on-chain cross-reference against local reth (zero Alchemy CU) -->

Method (new this turn): for each dropped opp, resolve the victim's **actual** block+txIndex on local reth, then check for a **post-victim, non-victim tx touching the same pool that also touches ≥2 distinct pools** (arb signature). This tests whether a drop was genuinely non-arbable or a real miss. (Two bugs fixed first: jq can't parse hex `transactionIndex`; ~10 victims land in `target_block+1`, so must use the victim's real block.)

Result — **most drops had no competitor (Ppost=0, single-venue), but 3 verified competitor arbs across all three buckets:**

| our verdict | competitor tx | to (contract) | distinct pools | gasUsed | read |
|---|---|---|---|---|---|
| `no_candidate_plans` (token `0x2b591e…`) | `0x7dfddfb8…` | `0x1f2f10d1` | 6 | 216k | multi-hop arb |
| `no-profitable-quote` | `0x69e97a78…` | `0x5d3a6d87` | 7 | 406k | multi-hop arb |
| `expired-before-solver` USDT/WETH | `0x3ab66d5a…` | `0xbc1d9760` | **25** | **3.29M** | large arb bot |

**Correction to the earlier funnel-only read:** "no_candidate = all longtail non-arbable" was wrong. Verified split:
- Dominant recurring cluster `0xE2a1437…`/token `0xc470f5E6` (7 opps): all `Ppost=0` → **genuinely non-arbable single-venue** (claim holds, now verified).
- ≥1 `no_candidate` (token `0x2b591e…`) had a real 6-pool competitor arb → **routable, our routing graph is missing venues** — a real coverage/planner gap, NOT longtail.

`not_seen` (Q1 — blocks where a competitor took an arb we never `opportunity_seen`): **not yet answered** — needs the reverse scan (enumerate window arb-signature txs, subtract our seen set). Folded into the Brief.

## Path / Leg Findings  <!-- auto -->

Solver-reaching opps (recoverable signal, n=6), now with competitor cross-reference:
- `solver | expired-before-solver` ×3 — **plans built (20 each)** but `oppTtlMs`(5000) elapsed before the solve loop. Pools `0xc7bBeC68…` (USDT/WETH), `0xE0554a47…` (USDC/WETH). Block 25434967: a **25-pool / 3.29M-gas** competitor arb landed right after our victim → **bluechip arb very likely lost to pre-solver latency.**
- `solver | no-profitable-quote` ×3 — quote search completed, no profitable amount; but a **7-pool competitor arb** landed on `0x41a561B9…` → either we mis-priced (haircut/safety too conservative) or they had a better route.

---

## Codex Round 1
- **core judgment:** (pending codex exec)
- **next_action:**
- **not_doing:**

## Claude Round 1
- **core judgment:** My first funnel-only read ("no_candidate = all longtail non-arbable") was unverified and partly wrong. The on-chain cross-reference proves competitor arbs exist in ALL three drop buckets (no_candidate/no-profit/expired), while the dominant recurring cluster genuinely is single-venue. The methodology gap is the real lesson: **we can't label a drop longtail-vs-real-gap without checking on-chain for a competitor take.** That check must be a repeatable every-run artifact, not a manual one-off.
- **next_action:** Build the competitor cross-reference (this run's manual forensics) into the analysis tool as the Hermes Competitor Coverage auto-section.
- **not_doing:** Not picking a searcher code fix (latency/coverage/economics) yet — with the cross-reference automated, the NEXT run's data decides which single bucket to attack; not over-fitting to n=3 per bucket.

## Codex Review Of Claude
- (pending codex exec)

## Claude Review Of Codex
- (see Claude Review Pass 1 below)

---

## Claude Final Decision  <!-- AUTHORITATIVE — only this drives code -->
- **decision:** The competitor cross-reference already EXISTS (`analysis live-loss` per-block + `--coverage`: `analyzeBlock`→`analyzeCompetitor`→`traceCompetitors`→`decideLoss`). My "build a tool" Brief was redundant — I failed to grep for it first (Codex, correctly, wrote no duplicate code). Evaluator gate on the node reproduced **2/3** manual hits and exposed one real bug: **`analyzeBlock` scans `target_block`, but ~10/26 victims land in `target_block+1`, so their competitor (incl. the 25-pool `0x3ab66d5a` on the expired bluechip) is silently missed.** Fix that one bug. Do NOT touch the searcher hot path.
- **rationale (1-2 lines):** A verified single-cause correctness bug in the existing loss-attribution tool: it trusts the event's predicted `target_block` instead of the victim's actual mined block, so it under-counts competitors on exactly the highest-value (block-slipped) opportunities. Small, surgical, and directly verifiable (0x3ab66d5a must appear after the fix).

## Implementation Brief
| task | owner | files | done-when |
|---|---|---|---|
| Resolve each opportunity's victim **actual block** via `rpc.getReceipt(victim_hash).blockNumber` and analyze competitors against THAT block, not the event's `target_block` | Codex | `analysis/src/cli/live-loss.ts` (`analyzeBlock`/`inferBlocks`/grouping ~203-252) | victim-in-`target_block±1` is scanned in its real block; preserve per-real-block receipt sharing |
| Keep the block+receipt fetch shared per **resolved real block** (group opportunities by victim's real block, fetch each once) so cost doesn't balloon | Codex | same | ≤ one `getBlockByNumber`+receipts per distinct real block |
| Robustness: victim with no receipt → mark `v_not_mined` and skip (no throw); hex block/index via existing `hexToNumber` | Codex | same | no crash on this run's 26 drops |
| (opt) coverage mode `runCoverageMode` uses the same real-block resolution for its watch scan | Codex | `analysis/src/cli/live-loss.ts` (~445) | consistent with per-block mode |

## Acceptance Criteria
1. `npm run build` (tsc `--noEmit`) clean in `analysis/`.
2. Re-run on the node against `/var/log/mev/events/searcher-live.jsonl` + local reth: the expired-before-solver opp (victim `0x7e870a5a…`, event `target_block` 25434966, **real block 25434967**) now surfaces competitor `0x3ab66d5a…` (25 pools); the 2 already-caught hits (`0x7dfddfb8…`, `0x69e97a78…`) stay caught; the `0xE2a1437…`/`0xc470f5E6` cluster stays `no_competitor`.
3. Read-only against RPC; no searcher-path changes; no Alchemy (local reth only).

---

## Implementation / Review-Fix Loop

Max **3** review/fix passes. Evaluator = non-author (Claude), must fill `ran_gate:` + `finding:` each pass; approve with neither is invalid.

### Codex Implementation Pass 1 (first Brief — "build the tool")
- **fixed:** nothing — Codex explored `analysis/` and found the competitor cross-reference already exists (`live-loss-report.ts`: `traceCompetitors`/`decideLoss`/`competitorCoverage`; `live-loss.ts`: `--events/--rpc/--coverage/--trace-top`). Correctly wrote no duplicate code (only `npm install ethers`). Valid generator outcome.
- **verification:** git status clean for `analysis/src`; grep confirmed pre-existing competitor logic.

### Claude Review Pass 1
- **ran_gate:** ran the existing tool on the node — `npm run live-loss -- --events /var/log/mev/events/searcher-live.jsonl --coverage --rpc http://127.0.0.1:8545` — then grepped `outputs/live-loss/*.md` for the 3 manual competitor hashes.
- **finding:** reproduces **2/3** — `0x7dfddfb8` (25434917) ✓, `0x69e97a78` (25434881) ✓; **misses `0x3ab66d5a`** because `analyzeBlock` scans `target_block` 25434966 while the victim landed in 25434967 (`target_block+1`). Root-caused to `analyzeBlock` (live-loss.ts:203-256) trusting `target_block` instead of the victim's real mined block. My original "build a tool" Brief was redundant (I skipped grepping existing code first).
- **blocking:** the `target_block` vs real-block mismatch (under-counts competitors on ~10/26 block-slipped opps, incl. the highest-value expired bluechip).
- **P1 in-scope:** fix block resolution in `analyzeBlock` (see revised Brief above).
- **P2/deferred:** Q1 not-seen reverse scan (already partly in `runCoverageMode`) — assess after the block-resolution fix.
- **approve_or_continue:** continue → revised Brief handed to Codex (pass 2).

### Codex Fix Pass 1 (pass 2 of the cycle)
- **fixed:** built a standalone `--competitor-scan` mode rather than editing `analyzeBlock` in place — new `analysis/src/competitor-scan.ts` (564 lines): per drop, resolves the victim's **real block** via `getReceipt(victim_hash).blockNumber`, pulls pool logs, flags post-victim non-victim txs touching ≥2 distinct pools, classifies `competitor_took`/`no_competitor`/`v_not_mined`, plus a bounded `--not-seen-scan` (Q1). Added `RpcClient.getLogs` + `getCode(blockTag)`, CLI flags + usage.
- **deviation:** did NOT fix the existing `analyzeBlock`/`--coverage` block-resolution bug (revised Brief asked for the in-place fix). The new mode sidesteps it correctly; the old per-block/coverage path still trusts `target_block`.
- **verification:** local `tsc --noEmit` clean.

### Claude Review Pass 2
- **ran_gate:** (1) `npm run build` (analysis, tsc --noEmit) → exit 0. (2) deployed the 3 changed files to the node and ran `npm run live-loss -- --competitor-scan --events /var/log/mev/events/searcher-live.jsonl --rpc http://127.0.0.1:8545` against local reth.
- **finding:** all 3 verified hits reproduced, incl. the block+1 case — `expired-before-solver` → `0x3ab66d5a` (25 pools) at **real block 25434967** (missed by `--coverage`); `no-profitable-quote` → `0x69e97a78` (7 pools); `no_candidate_plans` → `0x7dfddfb8` (6 pools). Table: no_candidate 1 took / 18 no_competitor / 1 v_not_mined → the `0xE2a1437`/`0xc470f5E6` cluster correctly in `no_competitor`. Hex via `quantityToNumber` (correct). Read-only, local reth only.
- **blocking:** none for `--competitor-scan` itself.
- **evaluator miss + self-correction:** I committed `a2099cd` after gating ONLY `--competitor-scan`. Codex had over-scoped — beyond `competitor-scan.ts` it also refactored `analyzeBlock`/`runCoverageMode` in `live-loss.ts` (~200 lines, unrequested). I did not review or gate that refactor before committing. On a follow-up gate the refactored **default mode failed** (`--block 25434966` still wrote `25434966-*.md` with `victim_not_landed`; no real-block resolution, `0x3ab66d5a` not surfaced). So the commit had mixed verified (`competitor-scan.ts`) and unverified/broken (`live-loss.ts` refactor) code.
- **fix:** reverted `live-loss.ts` to original + minimal `--competitor-scan` wiring only (commit follows); rebuilt (tsc clean) and re-ran the node gate — all 3 hits still reproduced. Verified surface only.
- **P2/deferred:** `analyzeBlock`/`runCoverageMode` real-block resolution — **owner: next cycle**, as a properly-scoped + gated change (not a bundled surprise).
- **approve_or_continue:** final_approval (of `--competitor-scan` only)

## Claude Final Approval
- **approved:** yes — `--competitor-scan` (module `competitor-scan.ts` + minimal CLI wiring) lands as the canonical, verified competitor cross-reference; Hermes Competitor Coverage auto-step: `analysis live-loss --competitor-scan --events <jsonl> --rpc <local-reth>`. The bundled `analyzeBlock`/coverage refactor was reverted (unverified).
- **follow-ups:** (1) next cycle: port real-block resolution into `analyzeBlock`/`--coverage`, scoped + gated. (2) process lessons (me): grep existing `analysis/` capability BEFORE a build-a-tool Brief (first Brief was redundant); and **gate the FULL diff, not just the mode I expected** — Codex changed more than the Brief asked, and I nearly landed it unverified (the Nodding/Blind-acceptance failure the acting-skeptic rule exists to catch).

---

## Next Run
- **next_state:** run a full 30-min dry-run with `--competitor-scan` wired into the auto-analysis, then attack the single highest-value VERIFIED competitor loss (expired-before-solver on bluechip — the 25-pool arb we lost to pre-solver latency).
- **live_allowed:** no   # dry-run only; go-live stays a human gate and requires a profitable on-fork sim
- **config changes:** none this turn (revm backend, oppTtl 5000, deadline 8000 unchanged)
- **what this run should answer:** with `--competitor-scan` now automated, which drop bucket has the most competitor-took losses over a full window — is it worth chasing coverage (no_candidate arbable misses) or latency (expired bluechip) first?
