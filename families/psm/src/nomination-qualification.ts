import { hashDomain } from "../../../packages/canonical-codec/src/index.ts";
export const PSM_NOMINATION_MUTATION_CORPUS = Object.freeze({ schemaVersion: 1, corpusRoot: hashDomain("aloha/psm/nomination-mutation-corpus/v1", ["unproven-log-surface", "foreign-raw-locator", "forged-call-semantics"]) });
export const PSM_NOMINATION_INDEPENDENT_ORACLE = Object.freeze({ schemaVersion: 1, oracleRoot: hashDomain("aloha/psm/nomination-independent-oracle/v1", ["no-owned-recent-pattern", "empty-safe-denominator"]) });
