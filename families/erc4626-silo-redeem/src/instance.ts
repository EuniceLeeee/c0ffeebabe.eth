import { hashDomain, type Hash } from "../../../packages/canonical-codec/src/index.ts";
import { assertCutoff, canonicalAddress, cutoffEqual, type Erc4626SiloRedeemIdentityV1, type Erc4626SiloRedeemMaterializedStateV1, type Erc4626SiloRedeemStateReadFactsV1 } from "./types.ts";
export type Erc4626SiloRedeemMaterializationOutcomeV1 = { readonly status: "verified"; readonly state: Erc4626SiloRedeemMaterializedStateV1 } | { readonly status: "chain-proven-rejected"; readonly reasonCode: "instance-mismatch" };
export function materializeErc4626SiloRedeem(input: { readonly identity: Erc4626SiloRedeemIdentityV1; readonly read: Erc4626SiloRedeemStateReadFactsV1 }): Erc4626SiloRedeemMaterializationOutcomeV1 { const read = { ...input.read, cutoff: assertCutoff(input.read.cutoff), instanceKey: canonicalAddress(input.read.instanceKey) }; if (!cutoffEqual(read.cutoff, input.identity.cutoff)) throw new TypeError("erc4626 silo state cutoff mismatch"); if (read.instanceKey !== input.identity.instanceKey) return { status: "chain-proven-rejected", reasonCode: "instance-mismatch" }; const stateHash = hashDomain("aloha/erc4626-silo-redeem/materialized-state/v1", { identityFactsHash: input.identity.factsHash, factsHash: read.factsHash }); return Object.freeze({ status: "verified", state: Object.freeze({ ...read, identityFactsHash: input.identity.factsHash, stateHash }) }); }
export function resealErc4626SiloRedeemState(state: Erc4626SiloRedeemMaterializedStateV1): Hash { return hashDomain("aloha/erc4626-silo-redeem/materialized-state/v1", { identityFactsHash: state.identityFactsHash, factsHash: state.factsHash }); }
