import { hashDomain, type Hash } from "../../../packages/canonical-codec/src/index.ts";
import { type Erc4626IdentityV1, type Erc4626RouteV1 } from "./types.ts";
export function deriveErc4626Routes(identity: Erc4626IdentityV1): readonly Erc4626RouteV1[] { const route = { instanceKey: identity.instanceKey, inputAsset: identity.facts.asset, outputAsset: identity.facts.asset }; return Object.freeze([Object.freeze({ ...route, routeBindingHash: hashDomain("aloha/erc4626/route-binding/v1", route) })]); }
export function assertErc4626Route(route: Erc4626RouteV1, identity: Erc4626IdentityV1): void { if (route.instanceKey !== identity.instanceKey || route.inputAsset !== identity.facts.asset || route.outputAsset !== identity.facts.asset) throw new TypeError("erc4626 route identity mismatch"); const expected = hashDomain("aloha/erc4626/route-binding/v1", { instanceKey: route.instanceKey, inputAsset: route.inputAsset, outputAsset: route.outputAsset }); if (route.routeBindingHash !== expected) throw new TypeError("erc4626 route binding mismatch"); }
