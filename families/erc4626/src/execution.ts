import { canonicalAddress, type Erc4626ActionV1, type Erc4626ExecutionIntentV1, type Erc4626IdentityV1 } from "./types.ts";
export function compileErc4626Execution(input: { readonly identity: Erc4626IdentityV1; readonly action: Erc4626ActionV1 }): Erc4626ExecutionIntentV1 { if (input.action.target !== canonicalAddress(input.identity.instanceKey)) throw new TypeError("erc4626 execution target mismatch"); return Object.freeze({ kind: "erc4626-execution-intent", cutoff: input.action.cutoff, target: input.action.target, calldata: input.action.calldata, actionHash: input.action.actionHash, exactQuoteHash: input.action.exactQuoteHash }); }
