import assert from"node:assert/strict";import test from"node:test";import{erc4626RoundingTolerance,withinErc4626UpperRoundingBound}from"../kernel/tolerance.ts";test("ERC4626 bound accepts exact edge and rejects one-unit mutation",()=>{const r=123_456n,e=r+erc4626RoundingTolerance(r);assert.equal(withinErc4626UpperRoundingBound(e,r),true);assert.equal(withinErc4626UpperRoundingBound(e+1n,r),false);});
