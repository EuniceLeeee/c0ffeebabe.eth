import assert from"node:assert/strict";import test from"node:test";import{directedPairs,metronomeSynthProjection,pairRequestId,type Address}from"../kernel/projection.ts";const a=(x:string)=>`0x${x.repeat(40)}`as Address,A=a("1"),B=a("2"),C=a("3");test("directed projection is canonical and direction-sensitive",()=>{const p=directedPairs([B,A,B]);assert.deepEqual(p,[{tokenIn:A,tokenOut:B},{tokenIn:B,tokenOut:A}]);assert.notEqual(pairRequestId("current",p[0]),pairRequestId("current",p[1]));assert.throws(()=>metronomeSynthProjection({pool:A,tokens:[A,B],activeDirections:[{tokenIn:A,tokenOut:C}],oracleBinding:"0x01"}));});
