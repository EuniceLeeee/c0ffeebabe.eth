import { canonicalAddress, type CurveActionV1, type CurveExecutionIntentV1, type CurveIdentityV1 } from "./types.ts";
export function compileCurveUnderlyingExecution(input: { readonly identity: CurveIdentityV1; readonly action: CurveActionV1 }): CurveExecutionIntentV1 { if (input.action.target !== canonicalAddress(input.identity.instanceKey)) throw new TypeError("curve execution target mismatch"); return Object.freeze({ kind: "curve-underlying-execution-intent", cutoff: input.action.cutoff, target: input.action.target, calldata: input.action.calldata, actionHash: input.action.actionHash, exactQuoteHash: input.action.exactQuoteHash }); }
