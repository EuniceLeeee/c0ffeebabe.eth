import assert from"node:assert/strict";import test from"node:test";import{decodeEigenpieQuote}from"../kernel/codec.ts";test("Eigenpie decodes exact canonical ABI words",()=>{const a=`0x${"11".repeat(20)}`,e=`0x${"7".padStart(64,"0")}${a.slice(2).padStart(64,"0")}`;assert.deepEqual(decodeEigenpieQuote(e),{amountOut:7n,tokenOut:a});assert.throws(()=>decodeEigenpieQuote(`${e}00`));});
