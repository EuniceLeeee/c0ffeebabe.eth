export function decodeEigenpieQuote(data:string):{readonly amountOut:bigint;readonly tokenOut:`0x${string}`}{if(!/^0x[0-9a-fA-F]{128}$/.test(data))throw new TypeError("quote must be exactly two ABI words");const word=data.slice(66);if(!/^0{24}/i.test(word))throw new TypeError("address word is not canonical");return Object.freeze({amountOut:BigInt(`0x${data.slice(2,66)}`),tokenOut:`0x${word.slice(24).toLowerCase()}`});}
