import { hashDomain, type Hash } from "../../../packages/canonical-codec/src/index.ts";
import { canonicalAddress, type EigenpieIdentityV1, type EigenpieRouteV1 } from "./types.ts";
export function deriveEigenpieRoutes(identity: EigenpieIdentityV1): readonly EigenpieRouteV1[] { const route = { instanceKey: identity.instanceKey, inputAsset: identity.facts.inputAsset, outputAsset: identity.facts.outputAsset }; return Object.freeze([Object.freeze({ ...route, routeBindingHash: hashDomain("aloha/eigenpie/route-binding/v1", route) })]); }
export function assertEigenpieRoute(route: EigenpieRouteV1, identity: EigenpieIdentityV1): void { if (route.instanceKey !== identity.instanceKey || canonicalAddress(route.inputAsset) !== identity.facts.inputAsset || canonicalAddress(route.outputAsset) !== identity.facts.outputAsset || route.inputAsset === route.outputAsset) throw new TypeError("eigenpie route identity mismatch"); const expected = hashDomain("aloha/eigenpie/route-binding/v1", { instanceKey: route.instanceKey, inputAsset: route.inputAsset, outputAsset: route.outputAsset }); if (route.routeBindingHash !== expected) throw new TypeError("eigenpie route binding mismatch"); }
