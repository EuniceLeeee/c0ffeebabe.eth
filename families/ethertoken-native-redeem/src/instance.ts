import { hashDomain, type Hash } from "../../../packages/canonical-codec/src/index.ts";
import { assertCutoff, canonicalAddress, cutoffEqual, type EtherTokenNativeRedeemIdentityV1, type EtherTokenNativeRedeemMaterializedStateV1, type EtherTokenNativeRedeemStateReadFactsV1 } from "./types.ts";
export type EtherTokenNativeRedeemMaterializationOutcomeV1 = { readonly status: "verified"; readonly state: EtherTokenNativeRedeemMaterializedStateV1 } | { readonly status: "chain-proven-rejected"; readonly reasonCode: "instance-mismatch" };
export function materializeEtherTokenNativeRedeem(input: { readonly identity: EtherTokenNativeRedeemIdentityV1; readonly read: EtherTokenNativeRedeemStateReadFactsV1 }): EtherTokenNativeRedeemMaterializationOutcomeV1 { const read = { ...input.read, cutoff: assertCutoff(input.read.cutoff), instanceKey: canonicalAddress(input.read.instanceKey) }; if (!cutoffEqual(read.cutoff, input.identity.cutoff)) throw new TypeError("ethertoken state cutoff mismatch"); if (read.instanceKey !== input.identity.instanceKey) return { status: "chain-proven-rejected", reasonCode: "instance-mismatch" }; const stateHash = hashDomain("aloha/ethertoken-native-redeem/materialized-state/v1", { identityFactsHash: input.identity.factsHash, factsHash: read.factsHash }); return Object.freeze({ status: "verified", state: Object.freeze({ ...read, identityFactsHash: input.identity.factsHash, stateHash }) }); }
export function resealEtherTokenNativeRedeemState(state: EtherTokenNativeRedeemMaterializedStateV1): Hash { return hashDomain("aloha/ethertoken-native-redeem/materialized-state/v1", { identityFactsHash: state.identityFactsHash, factsHash: state.factsHash }); }
