import { hashDomain } from "../../../packages/canonical-codec/src/index.ts";
import { validateEtherTokenNativeRedeem, type NativeDelta, type SupplyDelta, type TokenDelta } from "../kernel/effects.ts";
import { assertEtherTokenNativeRedeemRoute } from "./routes.ts";
import type { EtherTokenNativeRedeemIdentityV1, EtherTokenNativeRedeemQuoteV1, EtherTokenNativeRedeemRouteV1 } from "./types.ts";
export type EtherTokenNativeRedeemExactOutcomeV1 = { readonly status: "verified"; readonly quote: EtherTokenNativeRedeemQuoteV1 } | { readonly status: "chain-proven-rejected"; readonly reasonCode: "effect-conservation-failed" };
export function exactEtherTokenNativeRedeem(input: { readonly identity: EtherTokenNativeRedeemIdentityV1; readonly route: EtherTokenNativeRedeemRouteV1; readonly returnDataHex: string; readonly tokenDeltas: readonly TokenDelta[]; readonly nativeDeltas: readonly NativeDelta[]; readonly supplyDeltas: readonly SupplyDelta[]; readonly amountIn: string }): EtherTokenNativeRedeemExactOutcomeV1 { try { assertEtherTokenNativeRedeemRoute(input.route, input.identity); const amountIn = BigInt(input.amountIn); const amountOut = validateEtherTokenNativeRedeem({ completion: "returned", returnDataHex: input.returnDataHex, tokenDeltas: input.tokenDeltas, nativeDeltas: input.nativeDeltas, supplyDeltas: input.supplyDeltas, token: input.identity.facts.token, actor: input.identity.facts.actor, amountIn }); const payload = { cutoff: input.identity.cutoff, routeBindingHash: input.route.routeBindingHash, amountIn: amountIn.toString(), amountOut: amountOut.toString() }; return Object.freeze({ status: "verified", quote: Object.freeze({ ...payload, quoteHash: hashDomain("aloha/ethertoken-native-redeem/quote/v1", payload) }) }); } catch { return { status: "chain-proven-rejected", reasonCode: "effect-conservation-failed" }; } }
