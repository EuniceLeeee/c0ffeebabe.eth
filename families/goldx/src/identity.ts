import { hashDomain, type Hash } from "../../../packages/canonical-codec/src/index.ts";
import { GOLDX_FAMILY_AUTHORING_HASH } from "./family-definition.ts";
import { assertCutoff, canonicalAddress, cutoffEqual, type GoldxCandidateV1, type GoldxIdentityReadFactsV1, type GoldxIdentityV1 } from "./types.ts";
export type GoldxIdentityOutcomeV1 = { readonly status: "verified"; readonly identity: GoldxIdentityV1 } | { readonly status: "chain-proven-rejected"; readonly reasonCode: "reverse-identity-mismatch" };
export function verifyGoldxIdentityStage(input: { readonly candidate: GoldxCandidateV1; readonly reads: GoldxIdentityReadFactsV1 }): GoldxIdentityOutcomeV1 { const reads = { ...input.reads, cutoff: assertCutoff(input.reads.cutoff), target: canonicalAddress(input.reads.target), reverseTarget: canonicalAddress(input.reads.reverseTarget), inputAsset: canonicalAddress(input.reads.inputAsset), outputAsset: canonicalAddress(input.reads.outputAsset) }; if (!cutoffEqual(reads.cutoff, input.candidate.evidence.cutoff)) throw new TypeError("goldx identity cutoff mismatch"); if (reads.target !== canonicalAddress(input.candidate.target) || reads.reverseTarget !== reads.target) return { status: "chain-proven-rejected", reasonCode: "reverse-identity-mismatch" }; const facts = Object.freeze({ target: reads.target, inputAsset: reads.inputAsset, outputAsset: reads.outputAsset }); const factsHash: Hash = hashDomain("aloha/goldx/identity-facts/v1", facts); return Object.freeze({ status: "verified", identity: Object.freeze({ cutoff: reads.cutoff, candidateSnapshotHash: input.candidate.candidateSnapshotHash, instanceKey: reads.target, factsHash, facts }) }); }
export function identityDescriptorHash(identity: GoldxIdentityV1): Hash { return hashDomain("aloha/goldx/instance-descriptor/v1", { familyDefinitionHash: GOLDX_FAMILY_AUTHORING_HASH, factsHash: identity.factsHash, instanceKey: identity.instanceKey }); }
