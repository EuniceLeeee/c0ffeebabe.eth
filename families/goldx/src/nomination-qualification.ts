import { hashDomain } from "../../../packages/canonical-codec/src/index.ts";
export const GOLDX_NOMINATION_MUTATION_CORPUS = Object.freeze({ schemaVersion: 1, corpusRoot: hashDomain("aloha/goldx/nomination-mutation-corpus/v1", ["unproven-log-surface", "foreign-raw-locator", "forged-call-semantics"]) });
export const GOLDX_NOMINATION_INDEPENDENT_ORACLE = Object.freeze({ schemaVersion: 1, oracleRoot: hashDomain("aloha/goldx/nomination-independent-oracle/v1", ["no-chain-owned-event-abi", "empty-safe-denominator"]) });
