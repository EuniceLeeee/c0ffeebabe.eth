import { canonicalAddress, type DodoActionV1, type DodoExecutionIntentV1, type DodoIdentityV1 } from "./types.ts";
export function compileDodoExecution(input: { readonly identity: DodoIdentityV1; readonly action: DodoActionV1 }): DodoExecutionIntentV1 { if (input.action.target !== canonicalAddress(input.identity.instanceKey)) throw new TypeError("dodo execution target mismatch"); return Object.freeze({ kind: "dodo-v2-execution-intent", cutoff: input.action.cutoff, target: input.action.target, calldata: input.action.calldata, actionHash: input.action.actionHash, exactQuoteHash: input.action.exactQuoteHash }); }
