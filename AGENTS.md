# Codex Instructions

The canonical project instructions live in `CLAUDE.md`.

When reading this file, immediately read `CLAUDE.md` and follow it as the
source of truth for this repository.

When changing project instructions, edit `CLAUDE.md`. Keep this file as a
pointer unless the user explicitly asks otherwise.
